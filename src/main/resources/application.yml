spring:
  application:
    name: fleet_service_demo

  datasource:
    url: jdbc:mysql://localhost:3306/fleet_db
    username: root
    password: nitin@14
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.fleet_service_demo.entity

server:
  port: 8081

services:
  pricing:
    url: http://localhost:8082/api/v1/pricing